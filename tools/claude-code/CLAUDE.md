# Claude Configuration

This file controls Claude Code's behavior in this project.

## Project Overview
A playground for learning and experimenting with Claude Code.

## Behavior Guidelines

### Code Style
- Prefer concise, readable code over verbose solutions
- Add comments only where logic is non-obvious
- No unnecessary abstractions for one-time use

### Response Style
- Be brief and direct
- Skip preamble and filler phrases
- Lead with the answer, not the reasoning

### What to Avoid
- Don't add unrequested features or refactors
- Don't create new files unless necessary
- Don't add error handling for impossible scenarios
- No trailing summaries after completing a task

## Commands

### Common Tasks
- `npm install` — install dependencies
- `npm test` — run tests
- `npm run build` — build the project

## Notes
- Update this file as project conventions evolve
